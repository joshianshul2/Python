def Remove(list):
    final_list = []
    for num in list:
        if num not in final_list:
            final_list.append(num)
    return final_list


d= input()
for i in range(d):
  a=input().slpit()
  append(a)
print(Remove(list))

